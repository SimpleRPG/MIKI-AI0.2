import { repositoryArtifactAdapterService } from './repositoryArtifactAdapterService';
import type { NaturalLanguageDevelopmentUnderstanding } from './naturalLanguageDevelopmentIntentService';
export interface DevelopmentReadiness { ready:boolean; score:number; available:string[]; missing:string[]; blockers:string[]; }
class AutonomousDevelopmentReadinessService {assess(understanding:NaturalLanguageDevelopmentUnderstanding):DevelopmentReadiness {const available=['REPOSITORY_SEARCH','AST_TRANSFORM','CANDIDATE_WORKSPACE','VALIDATION','REPAIR','REVIEW_PACKAGE','REVISION_LIFECYCLE'];const missing:string[]=[];const blockers:string[]=[];for(const path of understanding.repositoryEvidence){const adapter=repositoryArtifactAdapterService.classify(path);if(!adapter.editable)missing.push(`ADAPTER:${adapter.kind}`);}if(understanding.ambiguities.length)blockers.push(...understanding.ambiguities);if(!understanding.repositoryEvidence.length&&understanding.primary.kind!=='GENERIC_FEATURE')blockers.push('NO_REPOSITORY_EVIDENCE');if(understanding.intents.some(intent=>intent.kind==='ASSET_CHANGE'))missing.push('BINARY_ASSET_GENERATION_OR_ENGINE_ADAPTER');const score=Math.max(0,Math.min(1,1-blockers.length*0.2-[...new Set(missing)].length*0.15));return {ready:blockers.length===0&&score>=0.7,score,available,missing:[...new Set(missing)],blockers};}}
export const autonomousDevelopmentReadinessService=new AutonomousDevelopmentReadinessService();
