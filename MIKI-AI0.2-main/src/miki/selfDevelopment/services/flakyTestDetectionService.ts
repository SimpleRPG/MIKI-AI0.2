import { canonicalSha256 } from '../../core/services/canonicalSha256Service';
class FlakyTestDetectionService{analyze(input:{testName:string;seed?:string;candidateSha256:string;environmentFingerprint:string;runs:Array<{passed:boolean;durationMs:number;error?:string}>}){const success=input.runs.filter(x=>x.passed).length,failed=input.runs.length-success;const durations=input.runs.map(x=>x.durationMs).sort((a,b)=>a-b);const flaky=success>0&&failed>0;return {flakyId:`FLAKY-${canonicalSha256(input).slice(0,24)}`,testName:input.testName,seed:input.seed,runCount:input.runs.length,successCount:success,failureCount:failed,duration:{min:durations[0]||0,median:durations[Math.floor(durations.length/2)]||0,max:durations.at(-1)||0},environmentFingerprint:input.environmentFingerprint,candidateSha256:input.candidateSha256,status:flaky?'FLAKY_TEST_SUSPECTED':failed?'CONSISTENT_FAILURE':'STABLE_PASS',errors:[...new Set(input.runs.map(x=>x.error).filter(Boolean))]};}}
export const flakyTestDetectionService=new FlakyTestDetectionService();
