import type { DevelopmentLifecycleState,DevelopmentReviewDecision,UnifiedDevelopmentRequest } from './unifiedDevelopmentRequestModel';
const transitions:Record<DevelopmentLifecycleState,DevelopmentLifecycleState[]>={CREATED:['WAITING_USER','PLANNING'],WAITING_USER:['PLANNING','PROMOTION_FAILED'],PLANNING:['IMPLEMENTING','PROMOTION_FAILED'],IMPLEMENTING:['VALIDATING','PROMOTION_FAILED'],VALIDATING:['REVIEW_READY','PROMOTION_FAILED'],REVIEW_READY:['ACCEPTED','REJECTED','REVISION_REQUESTED','PROMOTION_FAILED'],ACCEPTED:['COMPLETED'],REJECTED:['COMPLETED'],REVISION_REQUESTED:['REVISION_RUNNING'],REVISION_RUNNING:['REVIEW_READY','PROMOTION_FAILED'],PROMOTION_FAILED:['REVISION_REQUESTED'],COMPLETED:[]};
class DevelopmentLifecycleStateMachineService {transition(request:UnifiedDevelopmentRequest,next:DevelopmentLifecycleState):UnifiedDevelopmentRequest{if(!transitions[request.state].includes(next))throw new Error(`LIFECYCLE_TRANSITION_REJECTED:${request.state}->${next}`);return {...request,state:next,updatedAt:Date.now()};}canDecide(request:UnifiedDevelopmentRequest,decision:DevelopmentReviewDecision):boolean{return request.state==='REVIEW_READY'&&Boolean(request.workspaceId&&request.candidateId&&request.candidateSha256&&request.reviewPackageId)&&(decision!=='ACCEPT'||Boolean(request.candidateSha256));}}
export const developmentLifecycleStateMachineService=new DevelopmentLifecycleStateMachineService();
