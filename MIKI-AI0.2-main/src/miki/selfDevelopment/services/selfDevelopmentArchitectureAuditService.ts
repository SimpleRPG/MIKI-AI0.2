import { canonicalSha256 } from '../../core/services/canonicalSha256Service';
import { selfCodeSpaceService } from '../../core/services/selfCodeSpaceService';
export interface ArchitectureAuditFinding{code:string;severity:'INFO'|'WARNING'|'ERROR';paths:string[];detail:string;}
class SelfDevelopmentArchitectureAuditService{audit():{auditId:string;passed:boolean;findings:ArchitectureAuditFinding[]}{const files=selfCodeSpaceService.listSourceFiles();const findings:ArchitectureAuditFinding[]=[];const developmentEntries=files.filter(file=>/components\/.+\.tsx$/.test(file.path)&&/runAutonomousImplementation\(|developmentReviewLifecycleService\.start\(/.test(file.content));const legacyBypasses=developmentEntries.filter(file=>/runAutonomousImplementation\(/.test(file.content)&&!file.path.endsWith('CodeEditor.tsx'));if(legacyBypasses.length)findings.push({code:'GENERAL_UI_BYPASSES_LIFECYCLE',severity:'ERROR',paths:legacyBypasses.map(file=>file.path),detail:'General development UI must enter DevelopmentReviewLifecycle'});const codeEditor=developmentEntries.find(file=>file.path.endsWith('CodeEditor.tsx'));if(codeEditor)findings.push({code:'FILE_SCOPED_SELF_HEAL_ENTRY',severity:'INFO',paths:[codeEditor.path],detail:'Single-file self-heal intentionally uses original autonomous implementation service'});const serviceNames=new Map<string,string[]>();for(const file of files.filter(value=>value.path.endsWith('.ts'))){const name=file.path.split('/').pop()||file.path;serviceNames.set(name,[...(serviceNames.get(name)||[]),file.path]);}for(const [name,paths] of serviceNames)if(paths.length>1)findings.push({code:'DUPLICATE_SERVICE_FILENAME',severity:'WARNING',paths,detail:name});const lifecycle=files.find(file=>file.path.endsWith('/developmentReviewLifecycleService.ts'));if(!lifecycle||!lifecycle.content.includes('mikiSelfCodingSuperchargerService.runAutonomousImplementation'))findings.push({code:'ORIGINAL_AUTONOMOUS_PIPELINE_NOT_CONNECTED',severity:'ERROR',paths:lifecycle?[lifecycle.path]:[],detail:'Lifecycle must fall through to original autonomous implementation'});if(!lifecycle||!lifecycle.content.includes('multiIntentDevelopmentTransactionCoordinatorService.execute'))findings.push({code:'TRANSACTION_PREFLIGHT_NOT_CONNECTED',severity:'ERROR',paths:lifecycle?[lifecycle.path]:[],detail:'Semantic/component preflight must run before original pipeline'});return {auditId:`ARCH-${canonicalSha256(findings).slice(0,24)}`,passed:findings.every(item=>item.severity!=='ERROR'),findings};}}
export const selfDevelopmentArchitectureAuditService=new SelfDevelopmentArchitectureAuditService();
