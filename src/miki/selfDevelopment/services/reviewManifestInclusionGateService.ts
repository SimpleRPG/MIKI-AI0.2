import JSZip from 'jszip';import { canonicalSha256 } from '../../core/services/canonicalSha256Service';
class ReviewManifestInclusionGateService{async verify(zipBase64:string,expectedManifestId:string){const zip=await JSZip.loadAsync(zipBase64,{base64:true});const names=Object.keys(zip.files).filter(name=>name.endsWith('change_result_manifest.json'));if(names.length!==1)return {passed:false,reasons:[`MANIFEST_COUNT:${names.length}`],names};const content=await zip.files[names[0]].async('string');const manifest=JSON.parse(content);const reasons:string[]=[];if(manifest.manifestId!==expectedManifestId)reasons.push('MANIFEST_ID_MISMATCH');return {passed:reasons.length===0,reasons,names,manifestSha256:canonicalSha256(content),manifest};}}
export const reviewManifestInclusionGateService=new ReviewManifestInclusionGateService();
