import { isolatedCandidateWorkspaceService } from './isolatedCandidateWorkspaceService';
import { candidateValidationEvidenceService } from './candidateValidationEvidenceService';
import { singlePromotionGateService } from './singlePromotionGateService';
export interface ReviewCandidatePackage { formatVersion:1; workspaceId:string; issueId:string; generatedAt:number; decision:ReturnType<typeof singlePromotionGateService.evaluate>; files:Array<{path:string;baselineSha256:string;candidateSha256:string;candidateContent:string;evidenceIds:string[]}>; validationEvidence:ReturnType<typeof candidateValidationEvidenceService.list>; }
class ReviewCandidateExportService { create(workspaceId:string):ReviewCandidatePackage|undefined{const workspace=isolatedCandidateWorkspaceService.get(workspaceId);if(!workspace)return undefined;const decision=singlePromotionGateService.evaluate(workspaceId);if(!decision.allowed)return undefined;isolatedCandidateWorkspaceService.setStatus(workspaceId,'EXPORTED');return {formatVersion:1,workspaceId,issueId:workspace.issueId,generatedAt:Date.now(),decision,files:workspace.files.map(file=>({path:file.path,baselineSha256:file.baselineSha256,candidateSha256:file.candidateSha256,candidateContent:file.candidateContent,evidenceIds:[...file.evidenceIds]})),validationEvidence:candidateValidationEvidenceService.list(workspaceId)};}}
export const reviewCandidateExportService=new ReviewCandidateExportService();
