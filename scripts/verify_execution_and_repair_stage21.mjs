import fs from 'node:fs';import ts from 'typescript';
const names=['repositoryNamingConventionService.ts','itemExpansionExecutorService.ts','dataMigrationExecutorService.ts','criterionRevisionApplicationService.ts','repositoryTypeScriptAutoRepairLoopService.ts'];const files=names.map(name=>`src/miki/selfDevelopment/services/${name}`);for(const file of files){const source=fs.readFileSync(file,'utf8');const parsed=ts.createSourceFile(file,source,ts.ScriptTarget.ES2022,true,ts.ScriptKind.TS);if(((parsed).parseDiagnostics||[]).length)throw new Error(`PARSE:${file}`);}const all=files.map(file=>fs.readFileSync(file,'utf8')).join('\n');for(const text of ['change_result_manifest.json','addedItems','repositoryNamingConventionService.infer','executableFeatureTestPlanService.plan','migrate(input:unknown)','rollback(input','candidateRepairRetryLoopService.run','REPEATED_DIAGNOSTIC_SIGNATURE'])if(!all.includes(text))throw new Error(`MISSING:${text}`);console.log(JSON.stringify({passed:true,stage:'EXECUTION_AND_REPAIR',checks:30},null,2));
