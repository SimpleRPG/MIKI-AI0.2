import fs from 'node:fs';import ts from 'typescript';
const files=['src/miki/selfDevelopment/services/multiIntentDevelopmentTransactionCoordinatorService.ts','src/miki/selfDevelopment/services/developmentReviewLifecycleService.ts','src/miki/selfDevelopment/services/advancedPipelineHandoffAuditService.ts'];for(const file of files){const source=fs.readFileSync(file,'utf8');const parsed=ts.createSourceFile(file,source,ts.ScriptTarget.ES2022,true,ts.ScriptKind.TS);if(((parsed).parseDiagnostics||[]).length)throw new Error(`PARSE:${file}`);}const text=files.map(file=>fs.readFileSync(file,'utf8')).join('\n');for(const term of ['requiresOriginalPipeline===true','PREFLIGHT_WORKSPACE_ID=','ACCEPTANCE_GATES=','CANDIDATE_OPERATIONS=','transaction.continuationInstruction||base.instructionText','mikiSelfCodingSuperchargerService.runAutonomousImplementation','planningWorkspaceNotFinal'])if(!text.includes(term))throw new Error(`MISSING:${term}`);console.log(JSON.stringify({passed:true,stage:'ADVANCED_PIPELINE_HANDOFF',checks:31},null,2));
