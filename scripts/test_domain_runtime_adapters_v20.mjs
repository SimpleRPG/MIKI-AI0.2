import fs from 'node:fs';
const adapter=fs.readFileSync('src/miki/core/services/domainRuntimeAdapterService.ts','utf8');const stage=fs.readFileSync('src/miki/core/services/domainStageExecutionService.ts','utf8');const surface=fs.readFileSync('src/miki/core.ts','utf8');
const rows=[['core','taskBlackboardService.get'],['autonomy','capabilitySloService.evaluate'],['capability','capabilityGapService.getAllGaps/getAllProfiles'],['conversation','defaultConversationState/formatConversationStateForPrompt'],['data','deterministicModelCatalog manifest'],['execution','executionRunnerService.listRequests']].map(([domain,operation],index)=>({order:index+1,domain,operation,implemented:adapter.includes(`domain==='${domain}'`)&&adapter.includes(`'${operation}'`)}));
const checks={firstSixReal:rows.every(x=>x.implemented),remainingMarkedContractOnly:adapter.includes("'CONTRACT_ONLY','no-safe-runtime-adapter'"),stageExecutesAdapter:stage.includes('domainRuntimeAdapterService.execute(domain,taskId)'),workflowExported:surface.includes('domainSequentialWorkflowService'),adapterExported:surface.includes('domainRuntimeAdapterService')};const passed=Object.values(checks).every(Boolean);const report={version:'v20',passed,checks,realRuntimeAdapters:rows,remainingDomains:['experience','improvement','learning','memory','promotion','research','safety','selfAwareness','selfDevelopment','strategy','unknown','verification']};fs.writeFileSync('DOMAIN_RUNTIME_ADAPTER_AUDIT_V20.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!passed)process.exitCode=1;
