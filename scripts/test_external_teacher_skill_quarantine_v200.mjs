import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const s=readFileSync('server.ts','utf8');assert(s.includes("trustStatus?: 'UNVERIFIED' | 'VERIFIED' | 'REJECTED'"));assert(s.includes(".filter((s) => s.trustStatus === 'VERIFIED')"));assert(s.includes("trustStatus: 'UNVERIFIED'"));assert(s.includes("persistenceStatus: 'PROPOSAL_ONLY'"));assert(!s.includes('saveTeacherSkill(learnedSkill);'));assert(s.includes('includeQuarantined'));console.log('PASS V200');
