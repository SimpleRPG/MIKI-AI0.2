import fs from 'node:fs';import path from 'node:path';
const registry=fs.readFileSync('src/miki/core/services/domainServiceBindingRegistryService.ts','utf8');const stage=fs.readFileSync('src/miki/core/services/domainStageExecutionService.ts','utf8');const domains=['core','autonomy','capability','conversation','data','execution','experience','improvement','learning','memory','promotion','research','safety','selfAwareness','selfDevelopment','strategy','unknown','verification'];
const rows=domains.map((domain,index)=>{const re=new RegExp(`${domain}: \{ domain:'${domain}', serviceId:'([^']+)', modulePath:'([^']+)'`);const match=registry.match(re);let exists=false;if(match){const base='src/miki/core/services';exists=fs.existsSync(path.resolve(base,match[2]+'.ts'));}return {order:index+1,domain,bound:Boolean(match),moduleExists:exists,serviceId:match?.[1]};});
const checks={allBound:rows.every(x=>x.bound),allModulesExist:rows.every(x=>x.moduleExists),stageResolvesBinding:stage.includes('domainServiceBindingRegistryService.resolve(domain)'),failClosed:registry.includes('DOMAIN_SERVICE_UNAVAILABLE')};const passed=Object.values(checks).every(Boolean);const report={version:'v19',passed,checks,domains:rows};fs.writeFileSync('DOMAIN_SERVICE_BINDING_AUDIT_V19.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!passed)process.exitCode=1;
