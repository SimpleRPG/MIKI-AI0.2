import fs from 'node:fs';
const read=(p)=>fs.readFileSync(p,'utf8');
const ingress=read('src/miki/core/services/coreTaskIngressService.ts');const core=read('src/miki/core/services/coreOrchestratorService.ts');const router=read('src/miki/core/services/domainRouterService.ts');const completion=read('src/miki/core/services/coreCompletionGateService.ts');
const checks={ingressToCore:ingress.includes('coreOrchestratorService'),adaptiveRouting:core.includes('adaptiveRoutePlannerService'),notFixedSequential:!core.includes('EXECUTE_STAGE'),routerDispatch:core.includes('domainRouterService.dispatch'),blackboardReevaluation:core.includes('taskBlackboardService.get'),replyContract:router.includes('validateReply'),completionFailClosed:completion.includes('failClosed')||completion.includes('businessCompletion'),noObsoleteStageService:!fs.existsSync('src/miki/core/services/domainStageExecutionService.ts')};
const passed=Object.values(checks).every(Boolean);const report={version:'v63-adaptive-core',passed,checks};fs.writeFileSync('DOMAIN_STAGE_PIPELINE_AUDIT_V18.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!passed)process.exitCode=1;
