import fs from 'node:fs';import ts from 'typescript';
const files=['src/miki/selfDevelopment/services/developmentReviewLifecycleService.ts','src/miki/selfDevelopment/services/multiIntentDevelopmentTransactionCoordinatorService.ts','src/miki/selfDevelopment/services/generatedComponentVerificationRegistrationService.ts','src/miki/selfDevelopment/services/selfDevelopmentArchitectureAuditService.ts'];for(const file of files){const source=fs.readFileSync(file,'utf8');const parsed=ts.createSourceFile(file,source,ts.ScriptTarget.ES2022,true,ts.ScriptKind.TS);const errors=((parsed).parseDiagnostics||[]);if(errors.length)throw new Error(`PARSE:${file}:${errors.map(e=>e.messageText).join('|')}`);}const lifecycle=fs.readFileSync(files[0],'utf8');for(const term of ['multiIntentDevelopmentTransactionCoordinatorService.execute','mikiSelfCodingSuperchargerService.runAutonomousImplementation'])if(!lifecycle.includes(term))throw new Error(`LIFECYCLE_MISSING:${term}`);const tx=fs.readFileSync(files[1],'utf8');if(tx.includes("path:'review/multi_intent_transaction.json'"))throw new Error('GENERIC_PIPELINE_STILL_REPLACED_BY_MANIFEST_ONLY');if(!tx.includes('continueWithOriginalPipeline:true'))throw new Error('ORIGINAL_FALLTHROUGH_MISSING');const verify=fs.readFileSync(files[2],'utf8');for(const term of ['EXECUTION_EVIDENCE=${executionEvidence}',"staticPassed?'ANALYZED':'CANDIDATE'"])if(!verify.includes(term))throw new Error(`PROMOTION_GATE_MISSING:${term}`);console.log(JSON.stringify({passed:true,stage:'ARCHITECTURE_INTEGRATION',checks:24},null,2));
